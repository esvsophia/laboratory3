spring:
  application:
    name: missions-archive
  datasource:
    url: jdbc:postgresql://localhost:5432/missions_db
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB